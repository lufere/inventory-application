extends layout

block content
    h1= title

    ul
    each brand in brand_list
        li
            a(href = brand.url) #{brand.name }
            <br>
            | #{brand.description}
    else
        li No brands in database