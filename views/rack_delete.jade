extends layout

block content
    h1= title

    p Do you really want to delete this rack?
    h2 #{rack.brand?rack.brand.name:'Unbranded'} #{rack.type}

    form(method='POST' action='')
        input#rackid(type='hidden' name='rackid' value=rack._id)
        button.btn.btn-primary(type='submit') Delete